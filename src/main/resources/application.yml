spring:
  profiles:
    group:
      common:
        - db
        - security
      dev:
        - common
        - openapi
      prod:
        - common
        - openapi
      test:
        - common

logging:
  level:
    sql: debug