spring:
  profiles:
    group:
      common:
        - db
        - oauth
        - image
        - url
      dev:
        - common
        - openapi
      prod:
        - common
        - openapi
      test:
        - common

logging:
  level:
    sql: debug